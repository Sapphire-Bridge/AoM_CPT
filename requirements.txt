# Pinned top-level reviewer / CI dependencies for reproducible paper runs with venv + pip.
#
# Strict paper-facing clean-room reproduction uses `requirements.pip.lock.txt`.
# `requirements.lock.txt` is a provenance snapshot from a reference machine and includes
# Conda-only packages (for example `anaconda-anon-usage` and `conda`), so it is not used
# as the pip install surface.
torch==2.5.1
transformers==4.57.3
tokenizers==0.22.1
numpy==1.26.4
matplotlib==3.9.2
PyYAML==6.0.3
pytest==9.0.1
