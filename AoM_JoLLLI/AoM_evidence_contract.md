# JoLLI Evidence Contract (AoM)

This file is the canonical evidence table for the JoLLI AoM paper.

**Rule:** every empirical sentence in the manuscript should cite one or more Evidence IDs from this table.

Evidence ID namespaces:
- `E#`: empirical result claims backed by checked-in run artifacts (CSVs/JSON/PNGs).
- `R#`: run provenance / integrity facts backed by checked-in run manifests/logs.
- `C#`: implementation guarantees backed by code in this repo (what the pipeline does, not what any particular run did).

Conventions:
- **CI** is one of: `✓` (present for the primary metric), `varies` (present in some listed artifacts), `–` (not applicable / not reported).
- **Controls** lists run-time control conditions reported in the artifact (avoid mixing code-claims into `E#` rows).
- **Bootstrap unit** is the resampling unit used for the reported CIs (when applicable).
- **Artifact(s)** are repo-relative paths in this workspace.

## Canonical evidence table

| Evidence ID | Claim (allowed statement) | Metric / field(s) | Artifact(s) | Controls | CI | Bootstrap unit | Notes |
|---|---|---|---|---|---|---|---|
| `C1` | Dataset rows are validated at load time; invalid rows are counted (and sampled) and can be made fatal under strict policies. | `DatasetManifest: n_rows_{total,valid,invalid}`, `invalid_samples` (plus prompt/choice/label checks). | `aom/data/loaders.py`<br>`aom/data/validate.py`<br>`aom/data/dataset_manifest.py` | – | – | – | `invalid_samples` capped (5); error messages capped (500 chars). |
| `C2` | Runs seed RNGs and implement determinism modes `strict/best_effort/off`; strict determinism is rejected on MPS; determinism metadata is logged to manifests. | `seed_everything(...) → repro{seed, determinism_requested, determinism_enforced, determinism_reason, ...}` | `aom/repro.py`<br>`aom_eval.py` | – | – | – | Use CPU (or CUDA with deterministic flags) if strict determinism is required. |
| `C3` | CPT activation patching is implemented by hooking decoder-block outputs (“resid_post”) and replacing hidden states at the target span located via tokenizer offsets mapping; sham patching replaces receiver span with itself. | `PatchSpanSite`, `forward_with_patched_block_output_span`, `token_span_for_substring`, `require_token_id_match` default. | `aom/interventions/activation_patching.py`<br>`aom/token_spans.py`<br>`aom/metrics/disamb.py`<br>`aom_eval.py` | Sham patching; token-span alignment gate. | – | – | Layers configurable via `--patch_layers` (default: all). |
| `C4` | SDH/target-specificity control selects off-target control spans with buffered exclusions, adaptive `position_window`, deterministic selection seed, and strategy fallback (`matched_token → same_index → same_index_relaxed`). | `compute_cpt_target_specificity_control`, `_select_offtarget_pairs_with_fallback` | `aom/metrics/disamb.py`<br>`aom_eval.py` | Off-target control span patching; per-strategy deltas + counts. | – | – | Strategy counts are exported as `cpt_spec_n_directions_ctrl_patched_*`. |
| `C5` | DISAMB keyword baseline is implemented and can be computed/exported alongside DISAMB accuracy in AoM-eval outputs. | `baseline_disamb_keyword_accuracy` (and CI fields) | `aom/baselines.py`<br>`aom_eval.py` | Keyword heuristic baseline. | – | – | Baseline is a cue-vulnerability check; not intended as a competitive baseline. |
| `C6` | AoM-CF supports shift vs invariant (“sham”) items and computes shift-direction accuracy (primary) and split metrics. | `cf_shift_direction_accuracy`, `cf_*_shift_items`, `cf_*_invariant_items` | `aom/metrics/counterfactual.py`<br>`aom/data/validate.py` | Invariant (meaning-preserving) items. | – | – | Avoid claiming invariants exist in a run unless supported by an `E#`/artifact row. |
| `C7` | AoM-COH supports group variants (`main`, `ablate_relevant`, `ablate_irrelevant`) and paired deltas bootstrapped over `base_id`. | `coh_group_*`, `coh_paired_delta_*`, `coh_n_items_total_all_groups` | `aom/metrics/coherence.py` | Relevance-controlled ablations. | – | – | Avoid claiming group controls exist in a run unless supported by an `E#`/artifact row. |
| `C8` | Bootstrap CIs are percentile bootstrap CIs over the mean, with defaults `ci=0.95`, `n_bootstrap=1000`, `seed=42`, and explicit invalid/missing semantics for empty/non-finite samples. | `bootstrap_ci_metric`, `bootstrap_ci`, `MetricValue(valid, reason)` | `aom/stats.py` | – | – | – | Resampling unit is metric-specific (recorded in each `E#` row’s “Bootstrap unit”). |
| `C9` | COH pseudo-ablation patching protocol (padding strategy, irrelevant+sham controls, degradation-asymmetry diagnostic). | Manuscript protocol definition. | `AoM_JoLLLI/AoM_paper.md` | Irrelevant-span patching; sham patching. | – | – | See `E10c`/`E10d` for checked-in run artifacts using this protocol. |
| `C10` | CF intervention-span patching protocol (divergent span identification, `divergent_span_len_mismatch` skip rule, coverage constraint reporting). | Manuscript protocol definition. | `AoM_JoLLLI/AoM_paper.md` | Sham patching; skip-count reporting. | – | – | Implemented runs are covered by `E10a`/`E10b` artifacts. |
| `R0` | For the AoM-eval runs with checked-in manifests, all datasets had `n_rows_invalid=0` at load time (and invalid samples would be listed if present). | `datasets.{disamb,cf,coh}.n_rows_invalid`, `datasets.*.invalid_samples`, `data_error_policy` | `results/aom_eval.manifest.json`<br>`results/baseline_cpt_layer15.manifest.json`<br>`results/cpt_layer_sweep.manifest.json` | `data_error_policy` recorded (e.g. `warn_skip`). | – | – | Most other `results/*.csv` files do not have sibling manifests; invalid-row counts are not auditable from those artifacts alone. |
| `R2` | For the AoM-eval runs with checked-in manifests, determinism mode and enforcement status are explicitly recorded (MPS runs log a “not guaranteed” determinism reason). | `repro.{determinism_requested, determinism_enforced, determinism_reason}`, `device_backend` | `results/aom_eval.manifest.json`<br>`results/baseline_cpt_layer15.manifest.json`<br>`results/cpt_layer_sweep.manifest.json` | – | – | – | Use to support any run-specific determinism caveat sentence. |
| `R3` | In the “full-provenance” CSV artifacts, run configuration + environment fields are logged directly in the CSV (device/platform/versions/dtypes, bootstrap settings, strict finite checks, length normalization). | `platform`, `requested_device`, `device`, `python_version`, `torch_version`, `transformers_version`, `model_param_dtype`, `logprobs_dtype`, `bootstrap_n`, `bootstrap_seed`, `ci`, `strict_finite`, `no_length_norm` | `results/gpt2_results_cpt.csv`<br>`results/cpt_specificity_gpt2.csv`<br>`results/cpt_specificity_qwen3-4b.csv` | – | – | – | Use for run-specific provenance statements (and for `bootstrap_n/seed` “where recorded” claims). |
| `R4` | Some behavioral CSV artifacts omit most provenance fields (no device/dtype/versions/dataset hashes), so those aspects are not auditable from those CSVs alone. | Missing fields include `platform`, `python_version`, `torch_version`, `transformers_version`, `device`, `model_param_dtype`, `logprobs_dtype`, `bootstrap_n/seed`, `no_length_norm`, `strict_finite`. | `results/qwen_behavioral_cpu.csv`<br>`results/qwen_behavioral_plus_cpt_cpu.csv` | – | – | – | These artifacts still support behavioral and patching claims via `E1` and `E4a`, but provenance must be treated as “missing unless separately logged”. |
| `R5` | Exact `aom_eval.py` CLI invocations (argv) for some multi-model sweeps are captured in sweep launcher logs (including dataset paths, device/dtype, bootstrap settings, and `--local_files_only`). | `cmd=...` launcher lines (argv) + per-model stdout/stderr logs. | `results/mps_sweep_20260104_002404.launcher.log`<br>`results/logs/mps_sweep_20260104_002404/*.log` | – | – | – | Useful as a provenance “bridge” when a CSV lacks a sibling manifest (but does not retroactively add provenance to legacy CPU CSVs). |
| `E1` | AoM suite metrics (DISAMB/CF/COH + composite) with CIs are reported for Qwen2.5-{0.5B, 1.5B, 3B}. | `disamb_accuracy`, `disamb_n_pairs_total`, `disamb_n_sides_total`, `cf_shift_direction_accuracy`, `cf_n_items_total`, `cf_n_items_shift`, `cf_n_items_invariant`, `coh_constraint_accuracy`, `coh_n_items_total_all_groups`, `coh_group_main_n_items_total`, `coh_group_ablate_irrelevant_n_items_total`, `coh_group_ablate_relevant_n_items_total`, `aom_composite` | `results/qwen_behavioral_cpu.csv` | DISAMB baseline columns `baseline_disamb_*`<br>CF shift/invariant split columns `cf_*_{shift,invariant}_items`<br>COH ablation columns `coh_group_*`, `coh_paired_delta_*` | ✓ | DISAMB: pair<br>CF: item<br>COH: item (paired deltas: base_id) | This CSV does not record dataset paths/hashes/device/dtype/versions. Baseline columns are present in this artifact. |
| `E2a` | AoM suite metrics with CIs are reported for `openai-community/gpt2`. | `disamb_accuracy`, `disamb_n_pairs_total`, `disamb_n_sides_total`, `cf_shift_direction_accuracy`, `cf_n_items_total`, `cf_n_items_shift`, `cf_n_items_invariant`, `coh_constraint_accuracy`, `coh_n_items_total_all_groups`, `coh_group_main_n_items_total`, `coh_group_ablate_irrelevant_n_items_total`, `coh_group_ablate_relevant_n_items_total`, `aom_composite` | `results/gpt2_results_cpt.csv` | `baseline_disamb_*`, `cf_*_{shift,invariant}_items`, `coh_group_*` | ✓ | DISAMB: pair<br>CF: item<br>COH: item (paired deltas: base_id) | This artifact also contains CPT patching columns (`cpt_*`). |
| `E2b` | AoM suite metrics with CIs are reported for Llama-3.2 {1B, 1B-Instruct, 3B, 3B-Instruct}. | `disamb_accuracy`, `disamb_n_pairs_total`, `disamb_n_sides_total`, `cf_shift_direction_accuracy`, `cf_n_items_total`, `cf_n_items_shift`, `cf_n_items_invariant`, `coh_constraint_accuracy`, `coh_n_items_total_all_groups`, `coh_group_main_n_items_total`, `coh_group_ablate_irrelevant_n_items_total`, `coh_group_ablate_relevant_n_items_total`, `aom_composite` | `results/llama3_2_sweep_patching.csv` | `baseline_disamb_*`, `cf_*_{shift,invariant}_items`, `coh_group_*` | ✓ | DISAMB: pair<br>CF: item<br>COH: item (paired deltas: base_id) | Multi-row artifact; also contains CPT patching columns (`cpt_*`). |
| `E2c` | AoM suite metrics with CIs are reported for `meta-llama/Meta-Llama-3.1-8B-Instruct`. | `disamb_accuracy`, `disamb_n_pairs_total`, `disamb_n_sides_total`, `cf_shift_direction_accuracy`, `cf_n_items_total`, `cf_n_items_shift`, `cf_n_items_invariant`, `coh_constraint_accuracy`, `coh_n_items_total_all_groups`, `coh_group_main_n_items_total`, `coh_group_ablate_irrelevant_n_items_total`, `coh_group_ablate_relevant_n_items_total`, `aom_composite` | `results/llama3_1_8b_patching.csv` | `baseline_disamb_*`, `cf_*_{shift,invariant}_items`, `coh_group_*` | ✓ | DISAMB: pair<br>CF: item<br>COH: item (paired deltas: base_id) | This artifact also contains CPT patching columns (`cpt_*`). |
| `E2d` | AoM suite metrics with CIs are reported for `Qwen/Qwen3-4B`. | `disamb_accuracy`, `disamb_n_pairs_total`, `disamb_n_sides_total`, `cf_shift_direction_accuracy`, `cf_n_items_total`, `cf_n_items_shift`, `cf_n_items_invariant`, `coh_constraint_accuracy`, `coh_n_items_total_all_groups`, `coh_group_main_n_items_total`, `coh_group_ablate_irrelevant_n_items_total`, `coh_group_ablate_relevant_n_items_total`, `aom_composite` | `results/qwen3_4b_patching.csv` | `baseline_disamb_*`, `cf_*_{shift,invariant}_items`, `coh_group_*` | ✓ | DISAMB: pair<br>CF: item<br>COH: item (paired deltas: base_id) | This artifact also contains CPT patching columns (`cpt_*`). |
| `E3` | CPT activation patching summaries (effect + sham + skip counts) with CIs are reported for `openai-community/gpt2`. | `cpt_effect_layer_*`, `cpt_sham_effect_layer_*`, `cpt_mean_max_effect`, `cpt_mean_argmax_layer`, `cpt_flip_rate_at_best_layer`, `cpt_mean_sham_max_effect`, `cpt_n_directions_*` | `results/gpt2_results_cpt.csv` | Sham patching; token-span alignment gate; misalignment skips (`cpt_n_directions_skipped_misaligned`). | ✓ | direction | `cpt_n_directions_total=104`, `cpt_n_directions_patched=100`, `cpt_n_directions_skipped_misaligned=4`. Depth proxy used in the paper: `cpt_mean_argmax_layer`. Depth fraction convention: `depth_frac = layer_index / (n_blocks - 1)` where `layer_index` is 0-indexed. In these artifacts, `n_blocks` is auditable as `1 + max{i : cpt_effect_layer_i is finite}`. |
| `E4a` | CPT activation patching summaries (effect + sham) with CIs are reported for Qwen2.5-{0.5B, 1.5B, 3B}. | `cpt_effect_layer_*`, `cpt_sham_effect_layer_*`, `cpt_mean_max_effect`, `cpt_mean_argmax_layer`, `cpt_flip_rate_at_best_layer`, `cpt_mean_sham_max_effect`, `cpt_n_directions_*` | `results/qwen_behavioral_plus_cpt_cpu.csv` | Sham patching; misalignment skip counts (`cpt_n_directions_skipped_misaligned`). | ✓ | direction | Multi-row artifact (one row per model). Depth proxy used in the paper: `cpt_mean_argmax_layer`. Depth fraction convention: `depth_frac = layer_index / (n_blocks - 1)` where `layer_index` is 0-indexed. In these artifacts, `n_blocks` is auditable as `1 + max{i : cpt_effect_layer_i is finite}` (per row / model). |
| `E4b` | CPT activation patching summaries (effect + sham) with CIs are reported for `Qwen/Qwen3-4B`. | `cpt_effect_layer_*`, `cpt_sham_effect_layer_*`, `cpt_mean_max_effect`, `cpt_mean_argmax_layer`, `cpt_flip_rate_at_best_layer`, `cpt_mean_sham_max_effect`, `cpt_n_directions_*` | `results/qwen3_4b_patching.csv` | Sham patching; misalignment skip counts. | ✓ | direction | — |
| `E4c` | CPT activation patching summaries (effect + sham) with CIs are reported for `Qwen/Qwen3-4B-Instruct-2507`. | `cpt_effect_layer_*`, `cpt_sham_effect_layer_*`, `cpt_mean_max_effect`, `cpt_mean_argmax_layer`, `cpt_flip_rate_at_best_layer`, `cpt_mean_sham_max_effect`, `cpt_n_directions_*` | `results/qwen3_4b_instruct_2507_patching.csv` | Sham patching; misalignment skip counts. | ✓ | direction | — |
| `E4d` | CPT activation patching summaries (effect + sham) with CIs are reported for the Llama-3.2 family. | `cpt_effect_layer_*`, `cpt_sham_effect_layer_*`, `cpt_mean_max_effect`, `cpt_mean_argmax_layer`, `cpt_flip_rate_at_best_layer`, `cpt_mean_sham_max_effect`, `cpt_n_directions_*` | `results/llama3_2_sweep_patching.csv` | Sham patching; misalignment skip counts. | ✓ | direction | Multi-row artifact (one row per model). |
| `E4e` | CPT activation patching summaries (effect + sham) with CIs are reported for `meta-llama/Meta-Llama-3.1-8B-Instruct`. | `cpt_effect_layer_*`, `cpt_sham_effect_layer_*`, `cpt_mean_max_effect`, `cpt_mean_argmax_layer`, `cpt_flip_rate_at_best_layer`, `cpt_mean_sham_max_effect`, `cpt_n_directions_*` | `results/llama3_1_8b_patching.csv` | Sham patching; misalignment skip counts. | ✓ | direction | — |
| `E5a` | SDH/target-specificity control summaries (target vs off-target; delta + win rate) with CIs are reported for `gpt2`. | Run config: `cpt_spec_layer`, `cpt_spec_depth_frac`, `cpt_spec_position_window`, `cpt_spec_buffer`, `cpt_spec_donor_buffer`, `cpt_spec_selection_seed`.<br>Aggregates: `cpt_spec_mean_signed_target_effect`, `cpt_spec_mean_signed_ctrl_effect`, `cpt_spec_mean_signed_delta`, `cpt_spec_win_rate_signed` (plus `*_ci_low`/`*_ci_high` fields).<br>Stratified: `cpt_spec_mean_signed_delta_matched_token`, `cpt_spec_mean_signed_delta_same_index`, `cpt_spec_mean_signed_delta_same_index_relaxed` (plus `*_ci_low`/`*_ci_high` fields), `cpt_spec_n_directions_ctrl_patched_matched_token`, `cpt_spec_n_directions_ctrl_patched_same_index`, `cpt_spec_n_directions_ctrl_patched_same_index_relaxed`. | `results/cpt_specificity_gpt2.csv` | Off-target control span patching (`cpt_spec_*`), with per-strategy breakdown (`matched_token`, `same_index`, `same_index_relaxed`). | ✓ | direction | DISAMB-only artifact: CF/COH columns are zero-filled and `aom_composite` is not meaningful. |
| `E5b` | SDH/target-specificity control summaries with CIs are reported for Qwen2.5-{0.5B, 1.5B}. | Same columns as `E5a` (including stratified deltas + per-strategy Ns). | `results/qwen2.5_specificity_qwen2.5-0.5-1.5B.csv` | Same as `E5a`. | ✓ | direction | Multi-row artifact (one row per model). DISAMB-only caveat applies. |
| `E5c` | SDH/target-specificity control summaries with CIs are reported for `Qwen/Qwen2.5-3B`. | Same columns as `E5a` (including stratified deltas + per-strategy Ns). | `results/cpt_specificity_qwen2.5-3b.csv` | Same as `E5a`. | ✓ | direction | DISAMB-only caveat applies. |
| `E5d` | SDH/target-specificity control summaries with CIs are reported for `Qwen/Qwen3-4B`. | Same columns as `E5a` (including stratified deltas + per-strategy Ns). | `results/cpt_specificity_qwen3-4b.csv` | Same as `E5a`. | ✓ | direction | DISAMB-only caveat applies. |
| `E5e` | SDH/target-specificity control summaries with CIs are reported for the Llama-3.2 family. | Same columns as `E5a` (including stratified deltas + per-strategy Ns). | `results/cpt_specificity_llama3.2_family.csv` | Same as `E5a`. | ✓ | direction | Multi-row artifact. DISAMB-only caveat applies. |
| `E5f` | SDH/target-specificity control summaries with CIs are reported for `meta-llama/Meta-Llama-3.1-8B`. | Same columns as `E5a` (including stratified deltas + per-strategy Ns). | `results/cpt_specificity_llama3.1-8b_mps.csv` | Same as `E5a`. | ✓ | direction | DISAMB-only caveat applies. |
| `E7` | COH relevance-controlled ablation metrics (group accuracies + paired deltas) with CIs are present in these AoM-eval artifacts. | `coh_group_*`, `coh_paired_delta_*`, `coh_n_items_total_all_groups` | `results/gpt2_results_cpt.csv`<br>`results/qwen_behavioral_cpu.csv` | `group=main/ablate_relevant/ablate_irrelevant` | ✓ | group metrics: item<br>paired deltas: base_id | Some runs use COH datasets without groups (then `coh_n_items_total_all_groups == coh_n_items_total`). |
| `E8` | CF invariant (“sham”) item metrics with CIs are present (shift vs invariant split). | `cf_n_items_invariant`, `cf_mean_abs_shift_pref_invariant_items`, `cf_mean_abs_shift_pref_shift_items`, `cf_*_{shift,invariant}_items` | `results/gpt2_results_cpt.csv`<br>`results/qwen_behavioral_cpu.csv` | Invariant items vs shift items. | ✓ | item | Some runs use CF datasets with no invariant items (then invariant metrics are empty/invalid). |
| `E9` | DISAMB keyword baseline accuracy (with CI) is reported alongside model DISAMB accuracy. | `baseline_disamb_keyword_accuracy`, `disamb_accuracy` | `results/qwen_behavioral_cpu.csv` | Keyword heuristic baseline. | ✓ | pair | Baseline uses fixed cue lists (dataset-variant-sensitive). |
| `E10a` | CF intervention-span patching results (GPT‑2; shift items only under `span_mode=left_aligned_truncated`): mean max effect + sham baseline; N used/skipped. | `cf_patch_mean_max_effect`, `cf_patch_mean_max_effect_ci_low`, `cf_patch_mean_max_effect_ci_high`, `cf_patch_mean_sham_max_effect`, `cf_patch_n_cases_used`, `cf_patch_n_cases_total`, `cf_patch_n_cases_skipped_protocol`, `cf_patch_n_cases_skipped_span_mismatch`, `cf_patch_skip_reason_counts_json`, `cf_sha256`, `span_mode` | `results/paper_cf_patching_gpt2_trunc_hardened.csv`<br>`results/paper_cf_patching_gpt2_trunc_hardened.manifest.json` | Sham patching; skip reasons recorded. | ✓ | item | Uses the hardened CF dataset (`cf_sha256` recorded in the artifact). Artifact is CF-patching-only (behavioral AoM/CPT/SDH fields may be empty/zero-filled). |
| `E10b` | CF intervention-span patching results (Qwen2.5-{0.5B, 1.5B, 3B}; shift items only under `span_mode=left_aligned_truncated`): mean max effect + sham baseline; N used/skipped. | `cf_patch_mean_max_effect`, `cf_patch_mean_max_effect_ci_low`, `cf_patch_mean_max_effect_ci_high`, `cf_patch_mean_sham_max_effect`, `cf_patch_n_cases_used`, `cf_patch_n_cases_total`, `cf_patch_n_cases_skipped_protocol`, `cf_patch_n_cases_skipped_span_mismatch`, `cf_patch_skip_reason_counts_json`, `cf_sha256`, `span_mode` | `results/paper_cf_patching_qwen25_05b_trunc_si.csv`<br>`results/paper_cf_patching_qwen25_05b_trunc_si.manifest.json`<br>`results/paper_cf_patching_qwen25_15b_trunc_si.csv`<br>`results/paper_cf_patching_qwen25_15b_trunc_si.manifest.json`<br>`results/paper_cf_patching_qwen25_3b_trunc_si.csv`<br>`results/paper_cf_patching_qwen25_3b_trunc_si.manifest.json` | Sham patching; skip reasons recorded. | ✓ | item | Artifact is CF-patching-only (behavioral AoM/CPT/SDH fields may be empty/zero-filled). |
| `E10c` | COH pseudo-ablation patching results (GPT‑2): constraint-span vs irrelevant-span degradation with sham baselines; condition contrast is reported. | `coh_patch_stratum_condition__constraint_span_mean_max_effect` (and CI), `coh_patch_stratum_condition__irrelevant_span_mean_max_effect` (and CI), `coh_patch_stratum_condition__constraint_span_mean_sham_max_effect` (and CI), `coh_patch_comparison_condition_constraint_vs_irrelevant_mean_diff`, `coh_patch_comparison_condition_constraint_vs_irrelevant_cohens_d`, `coh_patch_n_cases_used`, `coh_patch_n_cases_total`, `coh_patch_protocol` | `results/paper_coh_patching_gpt2.csv`<br>`results/paper_coh_patching_gpt2.manifest.json` | Irrelevant-span patching; sham patching. | ✓ | case | CSV contains multiple seeds; patching summary fields are identical across seeds in this artifact. |
| `E10d` | COH pseudo-ablation patching results (Qwen2.5-{0.5B, 1.5B, 3B}): constraint-span vs irrelevant-span degradation with sham baselines; condition contrast is reported. | Same columns as `E10c`. | `results/paper_coh_patching_qwen25_0p5_1p5_3b_seed0.csv`<br>`results/paper_coh_patching_qwen25_0p5_1p5_3b_seed0.manifest.json` | Irrelevant-span patching; sham patching. | ✓ | case | Multi-row artifact (one row per model). |

## Optional / Not used for core JoLLI claims

The evidence IDs below are retained for future mechanistic extensions but are not required to support the core AoM/CPT/SDH claims in `AoM_paper.md`.

| Evidence ID | Claim (allowed statement) | Metric / field(s) | Artifact(s) | Controls | CI | Bootstrap unit | Notes |
|---|---|---|---|---|---|---|---|
| `E10` | Induction-head advantage (per layer/head) with bootstrap CIs is reported (example: GPT‑2). | `induction_advantage` (and CI fields) | `results/induction_gpt2.csv` | Baseline mode recorded (`baseline_mode`). | ✓ | synthetic sequence (batch element) | Additional `results/induction_*.csv` use the same schema. |
| `E11` | DISAMB logit-lens dataset summary (mean logit diff + bootstrap CI) is reported. | `mean_logit_diff`, `ci_low`, `ci_high`, `n_samples` | `results/logit_lens_disamb_table.csv`<br>`results/logit_lens_disamb_trace.csv`<br>`results/logit_lens_disamb.png` | Single-token continuation selection (some pair-sides may be skipped; reflected in `n_samples`). | ✓ | pair_side | Cite only if the manuscript makes an explicit logit-lens trajectory/curve claim. |
| `E12` | SAE feature-space CPT patching and a channel decomposition of CPT effect (with CIs) are reported for Gemma-2-2b. | `sae_cpt_*` (CSV), plus decomposition terms (`total_effect`, `recon_main_effect`, `resid_main_effect`, `interaction`) | `results/gemma2b_sae_cpt_layer15.csv`<br>`results/gemma2b_sae_cpt_layer18.csv`<br>`results/channel_decomp_layer13.json` | Sham patching; `results/channel_decomp_layer13.json` also includes `random_donor_sham`. | ✓ | direction | These artifacts include SAE config and (for the CSVs) dataset paths + SHA256s. |
