Post-submission, we audited claim-to-artifact alignment in the released repository. The main issue was an SDH/Table 3 provenance mismatch: the checked-in matched-token SDH artifact supports target-span concentration relative to nearby matched spans, but does not support the broader strategy-sensitivity language in the submitted text. We have therefore narrowed the SDH claim accordingly and clarified that layer-sweep summaries such as `mean_max_effect` and `mean_argmax_layer` are descriptive post-selection summaries rather than confirmatory estimates. Core CF and COH results are unchanged.
